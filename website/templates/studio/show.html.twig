{% extends 'base.html.twig' %}

{% block title %}Studio{% endblock %}

{% block body %}
    <h1>Studio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ studio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ studio.name }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ studio.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('studio_index') }}">back to list</a>

    <a href="{{ path('studio_edit', {'id': studio.id}) }}">edit</a>

    {{ include('studio/_delete_form.html.twig') }}
{% endblock %}
