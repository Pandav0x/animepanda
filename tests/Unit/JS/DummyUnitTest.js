'use strict';

const assert = require('chai').assert;

describe('Math Test Suite', function () {
    describe('Assertion on numbers', function () {
        it('Assert that 1 equals 1', function () {
            assert.equal(1, 1);
        });
    });
});